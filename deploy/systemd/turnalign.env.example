# Copy to /etc/turnalign/turnalign.env, replace the value, then chmod 0640.
# Generate at least 32 random bytes, for example with: openssl rand -hex 32
TURNALIGN_AUTH_TOKEN=replace-with-a-random-production-secret
